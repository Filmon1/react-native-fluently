// Centralized image imports. Import assets here and use them via `images.*`
// instead of requiring files directly inside screens/components.
import mascotLogo from "@/assets/images/moscot-logo.png";
import mascotWelcome from "@/assets/images/mascot-welcome.png";

export const images = {
  mascotLogo,
  mascotWelcome,
};